
  # NetCodeShop website

  This is a code bundle for NetCodeShop website. The original project is available at https://www.figma.com/design/ntHz5ZPsUQHr7gTRvoGKIL/NetCodeShop-website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  